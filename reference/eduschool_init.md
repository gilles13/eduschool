# Initialiser un contexte eduschool

Initialiser un contexte eduschool

## Usage

``` r
eduschool_init(fichier_base = ":memory:", importer = TRUE)
```

## Arguments

- fichier_base:

  Base DuckDB, en mémoire par défaut.

- importer:

  Importer les CSV dans DuckDB.
