# Examens modelises

Examens modelises

## Usage

``` r
examens(code = NULL, session = NULL)
```

## Arguments

- code:

  Code d'examen facultatif, par exemple \`"DNB"\`.

- session:

  Session facultative.

## Value

Un data.frame.
