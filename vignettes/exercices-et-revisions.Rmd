---
title: "Exercices et révisions"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Exercices et révisions}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>")
```

![Chaîne de génération des exercices et révisions](../man/figures/exercices_revisions.svg)

Le moteur d'exercices est déterministe lorsqu'un `seed` est fourni. Pour voir
immédiatement les énoncés dans la console :

```{r, eval=FALSE}
generer_fiche(
  "6E",
  "ITM_MAT_C3_6E_C09",
  n = 5,
  seed = 123,
  afficher = TRUE
)
```

## Produire une fiche HTML ou PDF

La sortie de `generer_fiche()` peut être envoyée directement vers un document :

```{r, eval=FALSE}
generer_fiche(
  "6E",
  "ITM_MAT_C3_6E_C09",
  n = 10,
  seed = 123
) |>
  produire_fiche("fiche_6e", format = "auto")
```

`format = "auto"` choisit le PDF si `pdflatex` est disponible sur la machine et
le HTML sinon. Il est également possible de choisir explicitement :

```{r, eval=FALSE}
generer_fiche("6E", "ITM_MAT_C3_6E_C09", n = 10, seed = 123) |>
  produire_fiche("fiche_6e", format = "html")
```

```{r, eval=FALSE}
generer_fiche("6E", "ITM_MAT_C3_6E_C09", n = 10, seed = 123) |>
  produire_fiche("fiche_6e", format = "pdf")
```

La sortie HTML ne nécessite pas LaTeX. La sortie PDF nécessite une installation
LaTeX fournissant `pdflatex`.

## Produire le corrigé

Le même lot d'exercices peut être envoyé au template de corrigé :

```{r, eval=FALSE}
generer_fiche("6E", "ITM_MAT_C3_6E_C09", n = 10, seed = 123) |>
  produire_corrige("corrige_6e", format = "auto")
```

Les anciennes fonctions de rapport LaTeX restent disponibles pour compatibilité,
mais `produire_fiche()` et `produire_corrige()` constituent l'interface conseillée
pour les nouveaux usages.
